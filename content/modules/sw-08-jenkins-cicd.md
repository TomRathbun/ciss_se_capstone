# Moved — CI/CD and GitLab

This module is now **[CI/CD and GitLab](sw-08-gitlab-cicd.md)** (`sw-08-gitlab-cicd`).

CISS does **not** teach Jenkins. Pipelines are **GitLab CI/CD** (`.gitlab-ci.yml`, jobs, runners). Interns who say “GitLab Actions” mean this module — GitLab’s names are pipeline / job / runner, not GitHub Actions.

Bookmarks to `sw-08-jenkins-cicd` should follow the link above.
